dropout_rate: 0.2
# Removed label_col; target label is now driven by configs/mapping.json

eval:
  batch_size: 512
  threshold: 0.5
hidden_dims:
- 64
- 32
vector_size: 64
scaler_type: robust
train:
  batch_size: 256
  epochs: 10
  lr: 0.001
  seed: 42
