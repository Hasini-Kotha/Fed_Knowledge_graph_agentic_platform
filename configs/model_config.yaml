# Model Configuration
model_type: "mlp"
input_dim: 128
hidden_dims: [64, 32]
dropout: 0.2
num_classes: 1

scaler_type: "standard"
vector_size: 64

train:
  epochs: 10
  batch_size: 256
  lr: 0.001

eval:
  batch_size: 512
  threshold: 0.5